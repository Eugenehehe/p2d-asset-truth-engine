(() => {
  const ds={valid:[],unresolved:[],newBins:[],bins:[],binColumn:'',typeColumn:''};
  const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
  const clean=v=>String(v??'').trim(), upper=v=>clean(v).toUpperCase().replace(/\s+/g,' '), compact=v=>upper(v).replace(/[^A-Z0-9]/g,'');
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const status=(text,type='info')=>{const el=q('#directRunStatus');if(el){el.textContent=text;el.dataset.type=type;}};
  const headersFor=source=>{const rows=state.sources[source]||[];return rows.length?Object.keys(rows[0]):[];};
  const selected=id=>q(id)?.value||'';
  function fillSelect(id,headers,preferred=[]){const el=q(id);if(!el)return;const old=el.value;el.innerHTML='<option value="">— select column —</option>'+headers.map(h=>`<option value="${esc(h)}">${esc(h)}</option>`).join('');if(headers.includes(old))el.value=old;else{const lower=headers.map(h=>clean(h).toLowerCase());for(const p of preferred){const exact=lower.indexOf(p);if(exact>=0){el.value=headers[exact];break;}}if(!el.value){for(let i=0;i<lower.length;i++){if(preferred.some(p=>lower[i].includes(p))){el.value=headers[i];break;}}}}}
  function refreshColumns(){const fh=headersFor('flow'),nh=headersFor('network');fillSelect('#directFlowSerial',fh,['serial number','serial','sn']);fillSelect('#directFlowBin',fh,['current bin','flowtrac bin','bin','location']);fillSelect('#directNetworkSerial',nh,['serial number','serial','sn']);fillSelect('#directNetworkDns',nh,['device name','dns name','dns','hostname','host name','name']);status(`Columns loaded. Flowtrac: ${fh.length}; Network: ${nh.length}. Verify the selections, then run.`);}
  function get(row,col){return col?clean(row[col]):'';}
  function serialVariants(v){const k=compact(v),s=new Set();if(!k)return s;s.add(k);k.startsWith('S')?s.add(k.slice(1)):s.add('S'+k);return s;}
  function networkIndex(rows,serialCol){const m=new Map();for(const r of rows)for(const k of serialVariants(get(r,serialCol)))if(!m.has(k))m.set(k,r);return m;}
  async function parseWorkbook(file){const buf=await file.arrayBuffer(),wb=XLSX.read(buf,{type:'array'}),ws=wb.Sheets[wb.SheetNames[0]];return XLSX.utils.sheet_to_json(ws,{defval:'',raw:false});}
  function guessBinColumn(headers){return headers.find(h=>['bin','bin name','location'].includes(clean(h).toLowerCase()))||headers.find(h=>clean(h).toLowerCase().includes('bin'))||'';}
  function guessTypeColumn(headers){return headers.find(h=>['type','bin type','bintype'].includes(clean(h).toLowerCase()))||headers.find(h=>clean(h).toLowerCase().includes('type'))||'';}
  function parseExistingBin(v,type=''){const raw=clean(v),text=upper(raw);return {raw,type:clean(type),number:(text.match(/^(\d{4})\s*\(/)||[])[1]||'',abbrev:(text.match(/\(([A-Z0-9]+)\s*-/)||[])[1]||'',prefix:(text.match(/^(.+)\)-/)||[])[1]?`${(text.match(/^(.+)\)-/)||[])[1]})`:'',suffix:compact((text.match(/\)-(.+)$/)||[])[1]||'')};}
  function parseDnsLocation(dns){const first=clean(dns).toLowerCase().split('.')[0].split('-')[0].replace(/[^a-z0-9]/g,'');if(!first)return null;let m=first.match(/^b(\d{4})(tr[a-z0-9]+|rm[a-z0-9]+|c[a-z0-9]+|\d{3,4}[a-z]?)/);if(m)return {buildingNumber:m[1],buildingAbbrev:'',location:compact(m[2]),derived:`${m[1]}-${upper(m[2])}`};m=first.match(/^([a-z]{2,10})(tr[a-z0-9]+|rm[a-z0-9]+|c[a-z0-9]+|\d{3,4}[a-z]?)/);return m?{buildingNumber:'',buildingAbbrev:upper(m[1]),location:compact(m[2]),derived:`${upper(m[1])} ${upper(m[2])}`}:null;}
  function alternatives(v){const s=compact(v),set=new Set([s]);let m=s.match(/^(\d{3})([A-Z]?)$/);if(m)set.add(`0${m[1]}${m[2]}`);m=s.match(/^0(\d{3})([A-Z]?)$/);if(m)set.add(`${m[1]}${m[2]}`);return set;}
  function buildingBins(parsed){if(!parsed)return[];return ds.bins.filter(b=>parsed.buildingNumber?b.number===parsed.buildingNumber:b.abbrev===parsed.buildingAbbrev);}
  function matchBin(parsed){if(!parsed)return {status:'UNPARSEABLE DNS',matches:[]};const locs=alternatives(parsed.location),matches=buildingBins(parsed).filter(b=>locs.has(b.suffix));return {status:matches.length===1?'MATCHED':matches.length===0?'BIN NOT FOUND':'AMBIGUOUS BIN',matches};}
  function deriveNewBin(parsed){if(!parsed||!parsed.location)return null;const sameBuilding=buildingBins(parsed);const prefixes=[...new Set(sameBuilding.map(x=>x.prefix).filter(Boolean))];if(prefixes.length!==1)return null;const types=[...new Set(sameBuilding.map(x=>x.type).filter(Boolean))];return {warehouse:'UFIT',active:'Active',type:types.length===1?types[0]:'',aisle:prefixes[0],section:'',level:'',bin:parsed.location,separator:'-',unit:'',max:'',type_status:types.length===1?'INFERRED':types.length>1?'AMBIGUOUS':'MISSING'};}
  async function loadBins(file){status('Reading Bin master…');const rows=await parseWorkbook(file),headers=rows.length?Object.keys(rows[0]):[];ds.binColumn=guessBinColumn(headers);ds.typeColumn=guessTypeColumn(headers);if(!ds.binColumn)throw new Error(`Could not detect Bin column. Headers: ${headers.join(', ')}`);ds.bins=rows.map(r=>parseExistingBin(r[ds.binColumn],ds.typeColumn?r[ds.typeColumn]:'' )).filter(x=>x.raw&&x.suffix&&(x.number||x.abbrev));q('#binsStatus').textContent=`${file.name} · ${ds.bins.length.toLocaleString()} usable bins · Bin column: ${ds.binColumn}${ds.typeColumn?` · Type column: ${ds.typeColumn}`:' · no Type column found'}`;status('Bin master loaded. Verify Direct comparison column mapping.','success');}
  function build(){try{const flow=state.sources.flow||[],network=state.sources.network||[];if(!flow.length)throw new Error('Flowtrac file is not loaded.');if(!network.length)throw new Error('Network Discovery file is not loaded.');if(!ds.bins.length)throw new Error('Existing Bin master is not loaded.');const fs=selected('#directFlowSerial'),fb=selected('#directFlowBin'),ns=selected('#directNetworkSerial'),nd=selected('#directNetworkDns');if(!fs||!fb||!ns||!nd)throw new Error('Select Flowtrac Serial/Bin, Network Serial, and Network Device Name / DNS.');status(`Running with Flow Serial=[${fs}], Flow Bin=[${fb}], Network Serial=[${ns}], Device Name/DNS=[${nd}]…`);const net=networkIndex(network,ns);ds.valid=[];ds.unresolved=[];ds.newBins=[];let matches=0,same=0;const newBinKeys=new Set();for(const r of flow){const serial=get(r,fs),current=get(r,fb);let nr;for(const k of serialVariants(serial)){nr=net.get(k);if(nr)break;}if(!nr)continue;matches++;const dns=get(nr,nd);if(!dns){ds.unresolved.push({serial,current_bin:current,dns:'',derived_location:'',reason:'DEVICE NAME / DNS MISSING',candidate_count:0,candidates:''});continue;}const parsed=parseDnsLocation(dns),result=matchBin(parsed);if(result.status==='MATCHED'){const full=result.matches[0].raw;if(compact(full)===compact(current)){same++;continue;}ds.valid.push({include:true,serial,current_bin:current,dns,derived_location:parsed.derived,to_bin:full});}else{const unresolved={serial,current_bin:current,dns,derived_location:parsed?.derived||'',reason:result.status,candidate_count:result.matches.length,candidates:result.matches.slice(0,10).map(x=>x.raw).join(' | ')};ds.unresolved.push(unresolved);if(result.status==='BIN NOT FOUND'){const nb=deriveNewBin(parsed);if(nb){const k=compact(nb.aisle+'-'+nb.bin);if(!newBinKeys.has(k)){newBinKeys.add(k);ds.newBins.push({...nb,include:true,dns_example:dns,derived_location:parsed.derived});}}}}}render();status(`Done. ${flow.length.toLocaleString()} Flowtrac rows, ${matches.toLocaleString()} serial matches, ${ds.valid.length.toLocaleString()} validated updates, ${ds.unresolved.length.toLocaleString()} unresolved, ${ds.newBins.length.toLocaleString()} derivable new bins, ${same.toLocaleString()} already matching.`,'success');}catch(err){console.error(err);status(err.message||String(err),'error');alert(err.message||String(err));}}
  function render(){q('#directCount').textContent=ds.valid.length;q('#unresolvedCount').textContent=ds.unresolved.length;q('#newBinCount').textContent=ds.newBins.length;q('#directTable').innerHTML=`<thead><tr><th><input id="directSelectAll" type="checkbox" checked></th><th>Serial</th><th>Flowtrac Bin</th><th>DNS</th><th>Validated To Bin</th></tr></thead><tbody>${ds.valid.map((x,i)=>`<tr><td><input type="checkbox" data-direct-include="${i}" checked></td><td>${esc(x.serial)}</td><td>${esc(x.current_bin)}</td><td>${esc(x.dns)}</td><td>${esc(x.to_bin)}</td></tr>`).join('')}</tbody>`;q('#unresolvedTable').innerHTML=`<thead><tr><th>Serial</th><th>Flowtrac Bin</th><th>DNS</th><th>Derived</th><th>Reason</th><th>Candidate Bins</th></tr></thead><tbody>${ds.unresolved.map(x=>`<tr><td>${esc(x.serial)}</td><td>${esc(x.current_bin)}</td><td>${esc(x.dns)}</td><td>${esc(x.derived_location)}</td><td>${esc(x.reason)}</td><td>${esc(x.candidates)}</td></tr>`).join('')}</tbody>`;q('#newBinTable').innerHTML=`<thead><tr><th><input id="newBinSelectAll" type="checkbox" checked></th><th>Aisle</th><th>Bin</th><th>Type</th><th>Type status</th><th>Full preview</th><th>DNS example</th></tr></thead><tbody>${ds.newBins.map((x,i)=>`<tr><td><input type="checkbox" data-newbin-include="${i}" checked></td><td>${esc(x.aisle)}</td><td>${esc(x.bin)}</td><td><input data-newbin-type="${i}" value="${esc(x.type)}" placeholder="Bin Type"></td><td>${esc(x.type_status)}</td><td>${esc(x.aisle+'-'+x.bin)}</td><td>${esc(x.dns_example)}</td></tr>`).join('')}</tbody>`;qa('[data-direct-include]').forEach(e=>e.onchange=()=>ds.valid[+e.dataset.directInclude].include=e.checked);qa('[data-newbin-include]').forEach(e=>e.onchange=()=>ds.newBins[+e.dataset.newbinInclude].include=e.checked);qa('[data-newbin-type]').forEach(e=>e.oninput=()=>ds.newBins[+e.dataset.newbinType].type=e.value);const all=q('#directSelectAll');if(all)all.onchange=()=>{ds.valid.forEach(x=>x.include=all.checked);render();};const allNew=q('#newBinSelectAll');if(allNew)allNew.onchange=()=>{ds.newBins.forEach(x=>x.include=allNew.checked);render();};}
  function downloadCsv(name,rows){const text=rows.map(r=>r.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\r\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+text],{type:'text/csv;charset=utf-8'}));a.download=name;a.click();URL.revokeObjectURL(a.href);}
  function exportNewBins(){const selectedRows=ds.newBins.filter(x=>x.include);if(!selectedRows.length){alert('No derivable new bins selected.');return;}const fallbackType=clean(q('#newBinType')?.value);const rows=[['Line',' Warehouse',' Active',' Type',' Aisle',' Section',' Level',' Bin',' Separator',' Unit of Measure',' Max Quantity'],['','Required. Warehouse must already exist.','(Active / InActive) Blank = Active','Required. Bin type must already exist.','Required','If set then it is added after aisle as part of the bin name','If set then it is added after section as part of the bin name','If set then it is added after level as part of the bin name','A separator between aisle, section, level, and bin','If not blank, unit of measure must exist and max quantity cannot be blank','If not blank, max quantity must be a positive integer value and unit of measure cannot be blank']];selectedRows.forEach(x=>rows.push(['','UFIT','Active',clean(x.type)||fallbackType,x.aisle,'','',x.bin,'-','','']));downloadCsv('ImportBins_Derived.csv',rows);const missing=selectedRows.filter(x=>!(clean(x.type)||fallbackType)).length;if(missing)status(`Exported ${selectedRows.length} new-bin rows. ${missing} row(s) still have blank Type and must be completed before Flowtrac import.`,'warning');else status(`Exported ${selectedRows.length} new-bin rows with Bin Type populated.`,'success');}
  function exportWorkbook(){const selectedRows=ds.valid.filter(x=>x.include);if(!selectedRows.length&&!ds.unresolved.length){alert('Run comparison first.');return;}const template=[['Line',' Serial',' To Warehouse',' To Bin'],['','Required. Must be an existing serial.','Required. Must be an existing warehouse.','Required. Must be an existing bin within selected warehouse.']];selectedRows.forEach(x=>template.push(['',x.serial,'UFIT',x.to_bin]));const unresolved=[['Serial','Current Flowtrac Bin','DNS','Derived Location','Reason','Candidate Count','Candidate Bins']];ds.unresolved.forEach(x=>unresolved.push([x.serial,x.current_bin,x.dns,x.derived_location,x.reason,x.candidate_count,x.candidates]));const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet(template),'Bin Update');XLSX.utils.book_append_sheet(wb,XLSX.utils.aoa_to_sheet(unresolved),'Unmatched DNS Bins');XLSX.writeFile(wb,'ImportSerialBinTransfer_Validated.xlsx');}
  q('#binsFile').onchange=async e=>{const f=e.target.files[0];if(!f)return;try{await loadBins(f);}catch(err){status(err.message,'error');}};q('#refreshDirectColumns').onclick=refreshColumns;q('#runDirectCompare').onclick=build;q('#exportFlowtracTemplate').onclick=exportWorkbook;q('#exportNewBins').onclick=exportNewBins;q('#flowFile').addEventListener('change',()=>setTimeout(refreshColumns,300));q('#networkFile').addEventListener('change',()=>setTimeout(refreshColumns,300));setTimeout(refreshColumns,500);
})();